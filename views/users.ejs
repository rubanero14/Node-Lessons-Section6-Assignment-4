<%- include('includes/head.ejs') %>
    <% if(names.length > 0){ %>
        <h1>List of Users</h1>
        <ul>
            <% names.forEach((name) => { %>
                <li><%= name.name %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <h1>No users found!</h1>
    <% } %>
<%- include('includes/end.ejs') %>